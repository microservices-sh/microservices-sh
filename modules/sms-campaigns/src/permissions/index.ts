export const permissions = [
  "sms-campaigns.read",
  "sms-campaigns.write",
  "sms-campaigns.dispatch",
  "sms-campaigns.admin",
  "sms-campaigns.extend",
  "sms-campaigns.observe"
] as const;

export const smsCampaignsPermissions = permissions;
