# Video Generation Use Cases

Framework-neutral use cases currently live in `src/service/index.ts`.

Do not import SvelteKit, Hono, provider clients, R2 clients, billing clients, or secret values directly in use cases.
