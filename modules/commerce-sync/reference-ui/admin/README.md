# Commerce Sync Admin Reference UI

Use this folder for admin route examples, table/list screens, forms, and action dialogs.

Expected permission: `commerce-sync.read` for read views and `commerce-sync.write` for mutations.
