# Booking Module Tests

Formal unit tests are pending. Current verification is through:

- `pnpm module:booking -- build`
- `pnpm template:booking -- check:spec`
- `pnpm --filter @microservices-sh/template-booking-sveltekit smoke:http <dev-url>`

