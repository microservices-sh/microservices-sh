# Customer Module Tests

Formal unit tests are pending. Current verification is through:

- `pnpm --filter @microservices-sh/customer check:spec`
- `pnpm --filter @microservices-sh/customer build`
- consuming template smoke tests
