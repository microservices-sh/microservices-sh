# Operator Work Use Cases

Use cases are framework-neutral. They validate inputs, call hooks, write through `OperatorWorkStore`, and emit module events. Route adapters own auth, redirects, and response shapes.
