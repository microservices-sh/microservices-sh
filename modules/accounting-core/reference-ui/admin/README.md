# Accounting Core Admin Reference UI

Use this folder for admin route examples, table/list screens, forms, and action dialogs.

Expected permission: `accounting-core.read` for read views and `accounting-core.write` for mutations.
