# Support Inbox Visitor Reference UI

Expected visitor surfaces:

- Widget bootstrap that reads `getWidgetConfig`.
- Quick action buttons for message shortcuts and links.
- Start/reuse conversation by project/session.
- Add visitor messages to an active conversation.
- Display assistant/agent/system messages from the conversation thread.

Visitor surfaces must not expose channel configuration, admin notes, ticket internals, or raw provider credentials.
