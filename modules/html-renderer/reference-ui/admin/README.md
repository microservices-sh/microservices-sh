# Html Renderer Admin Reference UI

Use this folder for admin route examples, table/list screens, forms, and action dialogs.

Expected permission: `html-renderer.read` for read views and `html-renderer.write` for mutations.
