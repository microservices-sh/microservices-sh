# Hr People Ops Admin Reference UI

Use this folder for admin route examples, table/list screens, forms, and action dialogs.

Expected permission: `hr-people-ops.read` for read views and `hr-people-ops.write` for mutations.
