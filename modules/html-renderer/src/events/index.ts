export const htmlRendererEvents = {
  "emitted": [
    "html-renderer.created",
    "html-renderer.updated",
    "html-renderer.document.created",
    "html-renderer.document.resolved",
    "html-renderer.document.deleted"
  ],
  "consumed": []
} as const;
