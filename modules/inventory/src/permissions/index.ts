export const inventoryPermissions = [
  "inventory.read",
  "inventory.write",
  "inventory.admin",
  "inventory.extend",
  "inventory.observe"
] as const;

export const permissions = inventoryPermissions;
