# Content CMS Tests

`src/content-cms.test.ts` covers the memory-backed service lifecycle.

Add D1 adapter tests only when a host template needs route-level proof against a real D1-compatible engine.
