# Booking Use Cases

Planned use cases:

- `getAvailability`
- `createBooking`
- `cancelBooking`
- `rescheduleBooking`
- `expireBookingHolds`
- `confirmPayment`

Use cases must return framework-neutral results. Route adapters convert those results into SvelteKit or Hono responses.
