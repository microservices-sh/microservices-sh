# Admin Reference UI

Admin: document queue, extraction status, runtime selector, confidence review,
source evidence, approval/rejection, and target record link after approval.

Use `FileText` or `ScanText` style navigation. Gate read views with
`document-extraction.read`; gate approval with `document-extraction.review`.
