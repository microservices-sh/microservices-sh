# Gateway Reference UI

Admin: API key inventory, rate-limit state, token exchange diagnostics, and public-boundary configuration.

Visitor: not applicable. Gateway is infrastructure for public API access, not a direct visitor screen.

Agentic: inspect keys and rate-limit state first. Require approval before creating API keys or issuing scoped tokens.
