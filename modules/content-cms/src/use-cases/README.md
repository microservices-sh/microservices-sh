# Content CMS Use Cases

Framework-neutral use cases currently live in `src/service/index.ts`.

Do not import SvelteKit, Hono, provider clients, upload clients, or secret values directly in use cases.
