# Video Generation Tests

`src/video-generation.test.ts` covers the memory-backed service lifecycle.

Add D1 adapter tests when a host template needs route-level proof against a real D1-compatible engine.
