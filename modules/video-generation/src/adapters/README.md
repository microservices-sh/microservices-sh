# Video Generation Adapters

This module ships memory and D1 store adapters.

Provider adapters are host-owned implementations of `VideoGenerationProvider`. Keep provider credentials, webhook signatures, polling schedules, and output byte downloads in route adapters or companion modules.
