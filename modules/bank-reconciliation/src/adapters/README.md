# Bank Reconciliation Adapters

- `memory-bank-reconciliation-store.ts` is used by unit tests and local composition.
- `d1-bank-reconciliation-store.ts` maps the module port to Cloudflare D1 tables from `migrations/0001_initial.sql`.
