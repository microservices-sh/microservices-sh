{
  "schemaVersion": "2026-06-13",
  "id": "auth",
  "name": "Auth",
  "version": "0.1.0",
  "status": "available",
  "class": "platform",
  "summary": "EdDSA service-token mint/verify, scope checks, and JWKS for auth-gated inter-service communication.",
  "runtime": {
    "language": "typescript",
    "platform": "cloudflare-workers",
    "frameworkNeutral": true,
    "routeAdapters": ["sveltekit", "hono-later"]
  },
  "entrypoint": "src/index.ts",
  "resources": [
    { "type": "d1", "binding": "DB", "tables": ["signing_keys", "domain_events"] }
  ],
  "permissions": ["auth.mint", "auth.verify", "auth.admin", "auth.extend", "auth.observe"],
  "connections": {
    "requires": [],
    "optional": ["audit-log"],
    "rpc": {
      "exposes": [
        { "method": "mintToken", "scope": "auth.mint", "public": false },
        { "method": "verifyToken", "scope": "auth.verify", "public": false },
        { "method": "getJwks", "scope": null, "public": true }
      ],
      "calls": []
    },
    "events": {
      "emits": ["auth.token_minted", "auth.key_rotated"],
      "consumes": []
    },
    "hookPoints": {
      "beforeMintToken": { "kind": "filter", "scope": "auth.extend" },
      "afterTokenMinted": { "kind": "observer", "scope": "auth.observe" }
    },
    "provides": { "hooks": [] }
  },
  "customization": {
    "default": "config-hooks",
    "supported": ["config", "hooks", "overlay", "fork"]
  },
  "approval": {
    "risk": "high",
    "requiresApprovalFor": ["migrations", "secrets", "signing-key-rotation", "production-deploy"]
  }
}
