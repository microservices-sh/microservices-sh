{
  "openapi": "3.1.0",
  "info": {
    "title": "microservices.sh Audit Log Module",
    "version": "0.1.0",
    "description": "Append-only audit trail. Recording is internal (queue consumer or direct call); listing is admin-gated."
  },
  "paths": {
    "/audit": {
      "get": {
        "summary": "List audit events (requires audit.read)",
        "responses": {
          "200": { "description": "Audit events" },
          "400": { "description": "Invalid filter" }
        }
      }
    }
  }
}
